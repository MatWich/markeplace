pipeline {
    agent any

    stages {
        stage("build") {
            steps {
                echo "LOL BUILD"
            }
        }
        stage("test") {
           steps {
                echo "LOL TESTS"
           }
        }
       stage("deploy") {
           steps {
                echo "LOL DEPLOY"
           }
       }
    }
}